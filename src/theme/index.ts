import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#0088ff'
    },
    // light: {
    //   main: "#eee",
    // },
    // dark: {
    //   main: "#333",
    // }
  }
})

export default theme